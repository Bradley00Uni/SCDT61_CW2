@model IEnumerable<OnlineShop2022.Models.ProductModel>

@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    @{
        foreach (var product in Model)
        {
            <div class="col-md-4">
                <div class="card" id="product @product.Id">
                    <img src="@product.ImagePath" class="card-img-top img-product-main" id="product @product.Id img" alt="@product.">
                    <div class="card-body">
                        <p class="card-text" id="product @product.Id desc">@product.Description</p>
                         <p class="card-text" id="product @product.Id price">£@product.Price</p>
                           <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="AddToShoppingCart" id="@product.Id button" asp-route-ProductId="@product.Id">Add to cart</a>
                    </div>
                </div>
            </div>
        }
    }
</div>